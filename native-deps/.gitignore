# Downloaded / built third-party artifacts (all re-created by fetch-deps.ps1).
espeak-ng-src/
onnxruntime/
runtime/
# The pinned OCR models, 9.80 MB, re-created by fetch-ocr-models.ps1. Their digests live in
# kokoro-ocr/src/lib.rs and in that script, so what is committed is the pin, not the weights.
ocr/
# Build output
build/
